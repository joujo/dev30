
{% extends "EshopStockBundle:Default:indexf.html.twig" %}


{% block container %}

    {% for categorie in categories %}

<div class="deals">
    <div class="container">
        <div class="deals-row">
            <div class="col-md-3 focus-grid">
                <a href="products.html" class="wthree-btn">
                    <div>

                        <img src="{{ asset('images/')}}{{ categorie.nomImage }}" alt="ajout" style="width: 50px; height: 50px"></div>
                    <h4 class="clrchg">{{ categorie.libelle }}</h4>
                </a>
            </div>







            <div class="clearfix"> </div>
        </div>
    </div>
</div>
    {% endfor %}

{% endblock container %}